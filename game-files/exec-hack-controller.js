/** @param {NS} ns */
export async function main(ns) {
    // const data = ['target-host', 'weaken']
    // ns.writePort(1, JSON.stringify(data))

    /*
    find a host,
    determine the action: weaken, grow, hack
    fire the guns
    wait until all reported back or objective accomplished before moving on
    // queue objectives
     */
    // WOW!
}